using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Class <c>BookshelfManager</c> handles the bookshelf. It shows how many books are left in the
/// bookshelf. Therefore, it updates all sections by removing the choosen book from the respective 
/// section.
/// </summary>
public class BookshelfManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
