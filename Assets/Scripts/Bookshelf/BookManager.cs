using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Class <c>BookManager</c> handles the books of the bookshelf. It checks from which section of the
/// bookshelf the choosen book was selected.
/// </summary>
public class BookManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
