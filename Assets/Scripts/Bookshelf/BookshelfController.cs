using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * Script to show how many books are left in all bookshelf sections. Therefore, it updates all
 * book sections by removing the book from the respective section.
 */
public class BookshelfController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
